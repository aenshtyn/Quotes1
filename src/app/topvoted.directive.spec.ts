import { TopvotedDirective } from './topvoted.directive';

describe('TopvotedDirective', () => {
  it('should create an instance', () => {
    const directive = new TopvotedDirective();
    expect(directive).toBeTruthy();
  });
});
